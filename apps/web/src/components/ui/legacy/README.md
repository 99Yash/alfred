# Legacy dimension primitives

These primitives preserve the older dimension visual grammar for the development styleguide. New production surfaces use the `App*` primitives from `../v2`.

`LegacyButton` is also used by the global error/not-found fallback because that fallback remains inside a dimension-styled `FrostPanel`. Do not add other production consumers without first deciding which visual grammar owns the surface.
