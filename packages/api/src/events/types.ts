/**
 * Shared browser-safe event schemas live in `@alfred/schemas`.
 * Keep this module as the API-local compatibility import path for server code
 * and for `@alfred/api` type exports.
 */
export * from "@alfred/schemas/events";
